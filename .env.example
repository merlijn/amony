AMONY_HOST_MEDIA_PATH=/path/to/your/media
AMONY_DATA=./data
AMONY_DOCKER_USER_ID=1000
AMONY_AUTH_ENABLED=true
AMONY_JWT_SECRET_KEY=your_key_here

DATABASE_USERNAME=postgres
DATABASE_PASSWORD=changeme

# Postgres backup (enable with: docker compose --profile backup up -d)
BACKUP_SCHEDULE=0 2 * * *       # Cron schedule (default: daily at 2 AM)
BACKUP_RETENTION_DAYS=7         # Days to keep old backups
BACKUP_ON_STARTUP=false         # Run a backup immediately when container starts

# Postgres restore (enable with: docker compose --profile restore up -d)
RESTORE_FILE=amony_20250101_020000.sql.gz  # Backup filename to restore (from BACKUP_HOST_PATH)